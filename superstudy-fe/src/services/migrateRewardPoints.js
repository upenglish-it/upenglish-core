/**
 * STUB: migrateRewardPointsToCentral
 *
 * This was a one-time migration utility that consolidated reward points from
 * per-user Firebase subcollections into a central NestJS reward_points collection.
 *
 * This migration has already been completed. This stub prevents Vite from
 * failing on the dynamic import() that references this module from DashboardPage.
 */

export async function migrateRewardPointsToCentral(onStatus) {
    onStatus?.('⚠️ Reward points migration has already been completed in the NestJS migration phase.');
}
