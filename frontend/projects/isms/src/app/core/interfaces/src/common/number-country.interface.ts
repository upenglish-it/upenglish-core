export interface INumberCountry {
  name: string;
  countryCode: string;
  flag: string;
  // length: Array<number> | number;
  countryCallingCode: string;
}

export type NumberTypes = {
  name: string;
  value: string;
};
