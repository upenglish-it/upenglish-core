import { TableBoxAnswerChoiceEditorDirectiveRenderer } from "./table-box-answer-choice-editor-renderer.directive";

describe("TableBoxAnswerChoiceEditorDirectiveRenderer", () => {
  it("should create an instance", () => {
    const directive = new TableBoxAnswerChoiceEditorDirectiveRenderer();
    expect(directive).toBeTruthy();
  });
});
